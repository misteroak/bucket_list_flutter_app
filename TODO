
Todo:
  ☐ Repository should technically return failures, not exceptions
  ☐ Repository should not access domain directly, but rather a data store object
  ☐ Make AppListItems a domain model
  ☐ Firebase
  ☐ Make global material cuppertino
  ☐ Figure out low performance of list page

Step 3: Multiple lists - release changes:
  ☐ Switch repository to stream
  ☐ For that used the streaming_s_p from pubdev